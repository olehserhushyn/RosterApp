import { TypedTemplate } from './typed-template';

describe('TypedTemplate', () => {
  it('should create an instance', () => {
    const directive = new TypedTemplate();
    expect(directive).toBeTruthy();
  });
});
